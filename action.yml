---
name: "MegaLinter Custom Flavor: MyRepos"
author: "Petr Ruzicka"
description: "MegaLinter Custom Flavor MyRepos"
outputs:
  has_updated_sources:
    description: "0 if no source file has been updated, 1 if source files has been updated"
runs:
  using: "docker"
  # You can manually update the Docker image version here, or use the latest tag
  image: "docker://ghcr.io/ruzickap/megalinter-custom-flavor-my-repos/megalinter-custom-flavor:latest"
branding:
  icon: "check"
  color: "green"
